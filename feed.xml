<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://csmsum.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://csmsum.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2024-11-12T01:05:21+00:00</updated><id>https://csmsum.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">SVM and Kernel Method</title><link href="https://csmsum.github.io/blog/2024/Variational-Inference/" rel="alternate" type="text/html" title="SVM and Kernel Method"/><published>2024-11-05T00:15:00+00:00</published><updated>2024-11-05T00:15:00+00:00</updated><id>https://csmsum.github.io/blog/2024/Variational-Inference</id><content type="html" xml:base="https://csmsum.github.io/blog/2024/Variational-Inference/"><![CDATA[<p>%%</p> <h1 id="central-limit-theorem-for-bnns-trained-with-variational-inference">Central Limit Theorem for BNNs trained with Variational Inference</h1> <h2 id="variational-inference-a-review-for-statisticians">Variational Inference: A Review for Statisticians</h2> <h3 id="bayesian-statistics-recap">Bayesian Statistics Recap</h3> <h2 id="1-train">1. Train</h2> <table> <tbody> <tr> <td>Instead of tuning $\mathbf{w}$, tune $p(\mathbf{w}</td> <td>\mathcal{D}_{\text{train}})$. The posterior distribution is:</td> </tr> </tbody> </table> \[p(\mathbf{w}|\mathcal{D}_{\text{train}}) = \frac{p(\mathcal{D}_{\text{train}}|\mathbf{w}) p(\mathbf{w})}{p(\mathcal{D}_{\text{train}})}\] <p>Calculating the posterior is often difficult since calculating the evidence $p(\mathcal{D}_{\text{train}})$ is nearly impossible.</p> <h2 id="2-inference">2. Inference</h2> <p>To approximate:</p> \[p(y|\mathcal{D}) = \int p(y|\mathbf{w}) p(\mathbf{w}|\mathcal{D}) d\mathbf{w} = E_{p(\mathbf{w}|\mathcal{D})}[p(\hat{y}|\mathbf{w})]\] <p>This can be approximated by:</p> \[LHS \approx \frac{1}{n} \frac{\sum_{s=1}^n w_i\cdot\hat{y}(x)}{\sum_{s=1}^n w_i}\] <p>where $\mathbf{w}<em>{i} \sim p(\mathbf{w}|\mathcal{D})$ and $\hat{y}</em>{\mathbf{w}}(x)=f(x,\mathbf{w})$. That is, replacing | Neural Network (NN) | Bayesian Neural Network (BNN)                    | |———————|————————————————–| | $\mathbf{w}$  | $p(\mathbf{w}|\mathcal{D})$                  | | $f_{\mathbf{w}}(x)$ | $\int f_{\mathbf{w}}(x) p(\mathbf{w}|\mathcal{D}) d\mathbf{w}$ |</p> <ol> <li> <table> <tbody> <tr> <td><strong>Sampling Methods</strong>: Sample $\mathbf{w}$ from $p(\mathbf{w}</td> <td>\mathcal{D})$ and compute $p(y</td> <td>\mathbf{w})$ for each sample.</td> </tr> </tbody> </table> </li> <li> <table> <tbody> <tr> <td><strong>Variational Methods</strong>: Model $p(\mathbf{w}</td> <td>\mathcal{D})$ using a parametrized distribution $q(\mathbf{w})$ and optimize to approximate the true posterior.</td> </tr> </tbody> </table> </li> </ol> <p>%%</p> <h1 id="variational-inferences-a-review-for-statistics">Variational Inferences: A Review for statistics</h1> <p>Consider a joint density of latent variables $\mathbf{z}$ and observations $\mathbf{x}$:</p> \[p(\mathbf{x}, \mathbf{z}) = p(\mathbf{x}|\mathbf{z}) p(\mathbf{z})\] <p>\documentclass{article} \usepackage{amsmath}</p> <p>\begin{document}</p> <h2 id="a-short-review-of-bayesian-statistics">A short review of Bayesian statistics</h2> <h3 id="weighted-average-of-means">Weighted average of means</h3> <p>One of the main premises of Bayesian statistics is that we set the population parameters as random variables. Note the expectation of bayes estimator is weighted average of the expectation of likelihood distribution and of prior distribution.</p> \[\mathbb{E}[\hat{\theta}_{\text{Bayes}}] = r_1 \mathbb{E}[\theta | \mathcal{D}] + r_2 \mathbb{E}[\theta_{\text{prior}}]\] \[\text{where } \mathbb{E}[\theta | \mathcal{D}]= \mathbb{E}[\hat{\theta}_{\text{MLE}}] \text{ (of frequentist)}\] <h3 id="how-to-calculate-predictive-posterior-distribution">How to calculate predictive posterior distribution</h3> \[p(\tilde{y}|y) = \int p(\tilde{y}, \theta|y)d\theta = \int p(\tilde{y}|\theta, y)p(\theta|y)d\theta \text{ }(\because \tilde{y}|\theta \perp \!\!\! \perp y|\theta)\] <table> <tbody> <tr> <td>where $p(\tilde{y}</td> <td>\theta)$ is likelihood and $p(\theta</td> <td>y)$ is posterior distribution.</td> </tr> </tbody> </table> <h3 id="numerical-integration">Numerical Integration</h3> <h4 id="monte-carlo">Monte Carlo</h4> <table> <tbody> <tr> <td>To calculate expectations of functions of the posterior distribution, $E[h(\theta)</td> <td>y]$, use Monte Carlo methods or importance sampling. This is available when we know the posterior distribution of parameters, $\theta$, which is easily sampled.</td> <td> </td> </tr> <tr> <td>$$E[h(\theta)</td> <td>y] = \int h(\theta)\cdotp(\theta</td> <td>y)d\theta$$</td> </tr> </tbody> </table> <ol> <li> <table> <tbody> <tr> <td>Sample $\theta_1, \cdots, \theta_N$ from posterior distribution $\theta</td> <td>y$.</td> </tr> </tbody> </table> </li> <li>calculate $h(\theta_1), \cdots, h(\theta_N)$.</li> <li> <table> <tbody> <tr> <td>$\frac{1}{n}\sum_{i=1}^{N}{h(\theta_i)} = \bar{h_{MC}} \rightarrow^{n\rightarrow\infty}\int h(\theta)p(\theta</td> <td>y)d\theta = E[h(\theta</td> <td>y)]$.</td> </tr> </tbody> </table> </li> </ol> <h4 id="rejection-sampling">Rejection Sampling</h4> <p>We try rejection sampling if we don’t know the prior distribution of $\theta$. We want to find the expectation of (the function of) the posterior distribution $h(\theta)$ or $h(\theta|\mathcal{D})$. WLOG,</p> \[\mathbb{E}[h(\theta)] = \int h(\theta) p(\theta|\mathcal{D}) \, d\theta\] <table> <tbody> <tr> <td>Since we cannot directly sample from $p(\theta</td> <td>\mathcal{D})$:</td> </tr> </tbody> </table> <ol> <li>Sample $\theta^<em>, \theta_1, \dots, \theta_S$, where $\theta^</em> \sim g(\theta)$, and $g(\theta)$ is the proposal density.</li> <li> <table> <tbody> <tr> <td>Choose $M$ such that $p(\theta</td> <td>\mathcal{D}) \leq M g(\theta)$ for all $\theta$.</td> </tr> </tbody> </table> </li> <li>Sample auxiliary variable $u_1, \dots, u_S \sim \text{Uniform}(0, 1)$. 4.     - If $u \leq \frac{p(\theta^<em>|\mathcal{D})}{M g(\theta^</em>)}$, accept $\theta^<em>$.    - If $u &gt; \frac{p(\theta^</em>|\mathcal{D})}{M g(\theta^<em>)}$, reject $\theta^</em>$.</li> <li>Repeat steps 3 and 4.</li> <li>Calculate: $$</li> </ol> <table> <tbody> <tr> <td>\frac{1}{n} \sum<em>{i=1}^{n} h(\theta_i) \approx \mathbb{E}</em>{R.S.}[h(\theta)] = \bar{h}_{R.S.} \rightarrow^{n\rightarrow\infty} \int h(\theta) p(\theta</td> <td>\mathcal{D}) \, d\theta = \mathbb{E}[h(\theta)].</td> </tr> </tbody> </table> \[Sampling auxiliary variable and accepting/declining $\theta^*$ is equivalent to accepting $\theta*$ with possibility $\frac{p(\theta^*|\mathcal{D})}{M g(\theta^*)}$. ### Variational Inference #### Intractability of Posterior The posterior density:\] <table> <tbody> <tr> <td>p(\mathbf{z}</td> <td>\mathbf{x}) = \frac{p(\mathbf{x}</td> <td>\mathbf{z}) p(\mathbf{z})}{p(\mathbf{x})}</td> </tr> </tbody> </table> \[is often intractable. #### Evidence Lower Bound (ELBO) To approximate the intractable posterior, maximize the ELBO:\] <table> <tbody> <tr> <td>\text{ELBO}(q) = \mathbb{E}_{q(\mathbf{z})}[\log p(\mathbf{x}</td> <td>\mathbf{z})] - \text{KL}(q(\mathbf{z})</td> <td> </td> <td>p(\mathbf{z}))</td> </tr> </tbody> </table> \[Maximizing ELBO is equivalent to minimizing the KL divergence between the approximate and true posterior. ## Advanced Inference Techniques ### Sampling Methods - Monte Carlo integration: Sample$\mathbf{z}$from$p(\mathbf{z}|\mathbf{x})$. - Importance Sampling: Use a proposal distribution$g(\mathbf{z})$to sample and weight accordingly. ### Markov Chain Monte Carlo (MCMC) - Use MCMC methods like Gibbs Sampling or Metropolis-Hastings when direct sampling is not feasible. ## Conclusion Variational Inference provides a powerful framework for approximating intractable posteriors, balancing computational efficiency and flexibility.\]]]></content><author><name></name></author><category term="Variational"/><category term="Inference"/><category term="Variational"/><category term="Inference"/><summary type="html"><![CDATA[Variational Inference.]]></summary></entry><entry><title type="html">SVM and Kernel Method</title><link href="https://csmsum.github.io/blog/2024/kernel-method/" rel="alternate" type="text/html" title="SVM and Kernel Method"/><published>2024-10-05T15:12:00+00:00</published><updated>2024-10-05T15:12:00+00:00</updated><id>https://csmsum.github.io/blog/2024/kernel-method</id><content type="html" xml:base="https://csmsum.github.io/blog/2024/kernel-method/"><![CDATA[<h1 id="kernel-based-learning">Kernel Based Learning</h1> <p>This post deals with kernel based learning that are largely used nowadays in many neural network models, starting from maximal margin classifier through support vector classifier/machine to how they are employed in feature maps in neural networks.</p> <h2 id="support-vector-machine-review">Support Vector Machine Review</h2> <p>Support vector machines (SVMs) was developed in 1990s. It is originally from ‘maximal margin classifier’, which was further developed into ‘support vector classifier’, leading to SVM.</p> <h3 id="separating-hyperplane-maximal-margin-classifier">Separating Hyperplane (Maximal Margin Classifier)</h3> <p>Suppose a $p$-dimensional space, a hyperplane is a flat affine subspace of dimension $p-1$.</p> <p>For example, in two dimensions, a hyperplane is defined by the equation</p> \[\beta_0 + \beta_1X_1 + \beta_2X_2 = 0\] <p>Without loss of generality(WLOG), we can extend to the $p$-dimensional setting:</p> \[\beta_0 + \beta_1X_1 + \beta_2X_2 + \cdots + \beta_pX_p = 0\] <p>Suppose there exists a point $ x_i \in \mathbb{R}^p $ , i.e. $ x_i = (x_1, x_2, \cdots, x_n) $ then the hyperplane divides the $p$-dimensional space into two halves s.t.</p> \[\begin{aligned} \text{(1) : } \beta_0 + \beta_1x_1 + \beta_2x_2 + \cdots + \beta_px_p &gt; 0 \\\ \text{(2) : } \beta_0 + \beta_1x_1 + \beta_2x_2 + \cdots + \beta_px_p &lt; 0 \end{aligned}\] <p>Based on the location of subspace, if we classify (1) as $y_i =1$ and (2) as $y_i=-1$, we may say that:</p> \[y_i(\beta_0 + \beta_1x_1 + \beta_2x_2 + \cdots + \beta_px_p) &gt; 0, \forall \mathbf{x}\in\mathbb{R}^p\] <h3 id="how-to-decide-classifier-with-lagrange-multiplier">How to decide classifier with Lagrange Multiplier</h3> <p>Alright, now let’s say that we have an observation $x^\ast$ Also, let $f(x^\ast)=\beta_0 + \beta_1x^\ast_1 + \beta_2x^\ast_2 + \cdots + \beta_px^\ast_p$. Then we can assign $x^\ast$ into class 1 or to class -1. That is,</p> \[\hat{y}^\ast = \text{sign}f(x^\ast)\] <p>You may easily think that if the magnitude of $f(x^\ast)$ is far from 0, then $x^\ast$ lies far from the hyperplane, and otherwise $x^\ast$ lies right beside of the (separating) hyperplane.</p> <p>The smallest distance from such $x^\ast$ from the hyperplane is called ‘margin’, so the choice of $f(x^\ast)$ maximizing the margin is called ‘maximal margin classifier’.</p> <p>Maximal margin classifier is at last decided by only a handful of data that are close to the hyperplane but not others, and we say that the data ‘support’ the maximal margin hyperplane.</p> <p>So what we basically want here is to maximize $f(x^\ast)$, but we need to be cautious of the choices of $\beta$s because $\lbrace\beta\rbrace_p=\lbrace1,1, \cdots ,1\rbrace$ is equivalent to $\lbrace\beta\rbrace_p = \lbrace2,2,\cdots,2\rbrace$, as they do not affect the sign of $f(x^\ast)$ but only the magnitude. So we constrain $\beta \text{ s.t.} \sum_{j=1}^p\beta_j^{2}=1$. That is,</p> \[\begin{aligned} &amp;\max_{\beta_0, \cdots, \beta_p}M \\ &amp;\text{subject to } \sum_{j=1}^p\beta_j^{2}=1 \\ &amp;\text{and } y_i(\beta_0 + \beta_1x^\ast_{i1} + \beta_2x^\ast_{i2} + \cdots + \beta_px^\ast_{ip}) \geq M \\ \end{aligned}\] <p>Let $\textbf{w} = \frac{1}{M}(\beta_1, \cdots \beta_p)^T$ and $b=\frac{\beta_0}{M}$. Then since $\bf{w^Tw}=\frac{1}{M^2}$ , it suffices to</p> \[\begin{aligned} &amp;\min_{\mathbf{w}, b}\frac{1}{2}\mathbf{w^T w} \\ &amp;\text{subject to } 1-y_i(\mathbf{w^T x} + b) \leq 0, \forall i = 1,\cdots,n \end{aligned}\] <p>for all data point $\lbrace x_i \rbrace _{i=1}^n$. To minimize the Lagrangian Function $\mathcal{L}$ :</p> \[\begin{aligned} \mathcal{L} = \frac{1}{2}\mathbf{w^T w} + \sum_{i=1}^{n}\alpha_i(1-y_i(\mathbf{w^T x_i} + b)) \\ \alpha_i \geq 0, \forall i = 1,\cdots,n \end{aligned}\] <p>The partial derivatives:</p> \[\begin{aligned} &amp;\frac{\partial\mathcal{L}}{\partial\mathbf{w}} = \mathbf{w} +\sum_{i=1}^{n}\alpha_i(-y_i)(\mathbf{x_i}) =0 \implies \mathbf{w} = \sum_{i=1}^{n}\alpha_i y_i \mathbf{x}_i\\ &amp;\frac{\partial}{\partial b} = \sum_{i=1}^{n} \alpha_iy_i = 0 \end{aligned}\] <p>From the above equation, you may see that $\mathbf{w} = \sum_{i=1}^{n}\alpha_i y_i \mathbf{x}_i$. Let’s substitute this $\mathbf{w}$ back to the $\mathcal{L}$. Then the Lagrangian</p> \[\begin{aligned} \mathcal{L} = -\frac{1}{2}\sum_{i=1}^{n}\sum_{j=1}^{n}\alpha_i\alpha_jy_iy_j\mathbf{x}^T\mathbf{x} + \sum_{i=1}^{n} \alpha_i \\ \text{subject to } \sum_{i=1}^n\alpha_i y_i = 0, \forall i = 1, \cdots, n \end{aligned}\] <p>However, the maximal margin classifier does not exists if the data overlaps. If an extraordinary point exists in the other side of the data, we cannot find a classifier since it does not exists. Instead, we can give some margin to the maximal margin classifier, and we say them ‘support vector classifier’.</p> <h3 id="support-vector-classifier">Support Vector Classifier</h3> <p>The core idea for support vector classifier is to give margin to each data point so that the equation makes sense even with some outliers (<strong>soft margin</strong>). To address the problem, the SVC suggest slack variable $\xi_i$. The modified equation is as following:</p> \[\begin{aligned} &amp;\max_{\beta_0, \cdots, \beta_p}M \\ \text{subject to } &amp;\sum_{j=1}^p\beta_j^2=1 \\ &amp;y_i(\beta_0 + \beta_1x^\ast_{i1} + \beta_2x^\ast_{i2} + \cdots + \beta_px^\ast_{ip}) \geq M(1 - \xi_i) \\ &amp;\xi_i \geq 0, \\ &amp;\sum_{i=1}^n\xi_n \leq const. \end{aligned}\] <p>Intuitively, We may see that if a datapoint $x_i$ lies near the borderline, the $\xi_i$ for that datapoint will be larger, leading to smaller $M(1-\xi_i)$. Also, if $x_i$ lies at the other side than where it was suppoossed to be, the $\xi_i$ will be larger than 1 so that the $M(1-\xi_i) &lt; 0$.</p> <p>Now, to maximize the equation, Let $\mathbf{w} = \frac{1}{M}(\beta_1, \cdots, \beta_p)^T$. This optimization problem is equvalent to minimizing:</p> \[\mathcal{L} = \frac{1}{2}\mathbf{w^T w} + C\left[ \sum_{i=1}^n\xi_i\right] + \sum_{i=1}^n\alpha_i\left[ 1 - \xi_i - y_i(\mathbf{w^T x_i} + b)\right] - \sum_{i=1}^n\eta_i\xi_i\] <p>where the regularization parameters for the lagrange are $\alpha_i, \eta_i$ for $i=1,\cdots,n$, and $C$ is the regularization parameter or the penalty term for the equation. $C$ is hyperparameter WE choose how much to penalty the classification errors.</p> <p>Again, the partial derivatives are:</p> \[\begin{equation} \begin{aligned} &amp;\frac{\partial}{\partial\mathbf{w}}\mathcal{L} = \mathbf{w} +\sum_{i=1}^{n}\alpha_i(-y_i)(\mathbf{x_i}) =0 \implies \mathbf{w} = \sum_{i=1}^{n}\alpha_i y_i \mathbf{x}_i\\ &amp;\frac{\partial}{\partial b} = \sum_{i=1}^{n} \alpha_iy_i = 0 \\ &amp;\frac{\partial}{\partial\xi_i}\mathcal{L} = C - \alpha_i - \eta_i = 0 \iff \alpha_i = C-\eta_i \end{aligned} \end{equation}\] <p>Using the above equation, let’s go back to $\mathcal{L}$. Then it is the dual optimization problem (Proofs to be updated):</p> \[\begin{aligned} &amp;\text{Maximize } \mathcal{L} = \sum_{i=1}^n\alpha_i-\frac{1}{2}\sum_{i=1}^n\sum_{j=1}^n\alpha_i\alpha_jy_iy_j\mathbf{x_i^T x_j} \\ &amp;\text{subject to } \sum_{i=1}^n\alpha_iy_i=0, C\geq\alpha_i\geq0,(\eta_i = C-\alpha_i), \forall i = 1,\cdots,n \end{aligned}\] <p>The following conditions are called Karush-Kuhn-Tucker (KKT) conditions.</p> \[\begin{aligned} \alpha_i\left[1-\xi_i-y_i(\mathbf{w^T x_i} + b)\right] = 0 \\ \eta_i\xi_i = 0, \forall i=1,\dots,n, \\ yi(\mathbf{w^T x_i + b}) \geq 1 - \xi_i, \forall i = 1,\cdots,n. \end{aligned}\] <p>Since from the above equation of $\frac{\partial}{\partial \mathbf{w}}\mathcal{L}$, the solution is:</p> \[\begin{aligned} \mathbf{\hat{w}} = \sum_{i=1}^n \hat{\alpha_i}y_ix_i \end{aligned}\] <p>Therefore,</p> \[\hat{Y}(\mathbf{x}) = sign\left[\hat{\mathcal{f}}(\mathbf{x})\right] = sign\left[\mathbf{\hat{w}^T x} + \hat{b}\right]\] <p>To be updated more. \ref{eq1} asdf</p> <h3 id="support-vector-machine">Support Vector Machine</h3> <p>However, data does not always are classifiable with a line. In much more case, they are nonlinearly distributed. We may try some quadtratic terms in the support vector classifier, but this makes computation extremely difficult.</p> <p>The Kernel Method appears in that regards.</p> <ul> <li>We want to enlarge the feature space with higher-order (polynomial) terms</li> <li>We want the equation be simple nevertheless.</li> </ul> <h3 id="brief-conclusion">Brief Conclusion</h3> <p>We can show that the support vector classifier can be written as:</p> \[\begin{aligned} f(x) &amp;= \sum_{i=1}^n\alpha_iy_i&lt;\mathbf{x,x_i}&gt; + b \\ &amp;= \sum_{i\in S}^n\alpha_iy_i&lt;\mathbf{x,x_i}&gt; + b \end{aligned}\] <p>Let $\phi$ a $\mathbb{R}^p\rightarrow \mathbb{R}^M$ ($\mathbb{R}^p\rightarrow \mathcal{H}$ to be precise) function such that $\phi(\mathbf{x})=(\phi_1(\mathbf{x}),\cdots,\phi_M(\mathbf{x}))$ that moves each datapoint into (possibly high-dimensional) feature space. The inner product of such space will be</p> \[\begin{aligned} f(x) &amp;= \sum_{i=1}^n\alpha_iy_iK&lt;\mathbf{x,x_i}&gt;_\mathcal{H} + b \\ &amp;= \sum_{i\in S}^n\alpha_iy_iK&lt;\mathbf{x,x_i}&gt; + b \end{aligned}\] <p>So if we find adequate $\phi$ where the feature maps’ inner product is can be expressed as the original data’s inner product, it is equivalent to calculating on higher ($M$ in this case) dimension, but maintaining the calculation simple.</p> <p><strong>EXAMPLES and MORE EXPLANATIONS FOR KERNELS TO BE UPDATED</strong></p> <h3 id="acknowledges">Acknowledges</h3> <p>I would like to express my sincere gratitude to Professor <a href="https://sites.google.com/view/wonkukkim/home">Wonkuk Kim</a> at Chung-Ang University, as this post is largely based on insights gained from his Data Mining class.</p>]]></content><author><name></name></author><category term="kernel"/><category term="method"/><category term="Kernel"/><category term="Method"/><summary type="html"><![CDATA[Kernel Method explained with SVM.]]></summary></entry></feed>